import 'package:flutter/material.dart';

class BadSubject extends StatelessWidget {
  const BadSubject({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Bad Subject');
  }
}
