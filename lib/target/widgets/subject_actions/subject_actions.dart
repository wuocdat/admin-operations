import 'package:flutter/material.dart';

class SubjectActions extends StatelessWidget {
  const SubjectActions({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Subject Actions');
  }
}
